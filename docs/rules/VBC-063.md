---
title: VBC-063 - empty-else
editLink: false
---

# VBC-063: empty-else

<badge type="warning" text="warning" />

**Category:** clean-code  
**Analysis:** `Regex` (line by line)  
**File types:** `.js`, `.ts`, `.py`  
**Scope:** source code only — matches inside strings and comments are ignored  
**Tags:** `clean-code`

## What it reports

Empty else block detected at line {line}. This clutters the code.

## Flagged

```js
if (ready) { start(); } else {}
```

## Not flagged

```js
if (ready) { start(); } else { stop(); }
```

## Pattern

```regex
else\s*\{\s*\}
```
