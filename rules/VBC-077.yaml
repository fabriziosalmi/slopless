id: VBC-077
name: deceptive-function-name
severity: error
tags:
  - clean-code
  - semantic
match:
  ast_check:
    type: lying-function-names
message: >-
  Lying function name detected: '{name}' implies a non-mutating action but
  contains side-effect keywords (set, delete, update). This is major vibecoding.
category: heuristics
