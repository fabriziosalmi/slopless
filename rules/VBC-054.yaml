id: VBC-054
name: unnecessary-else
severity: warning
tags:
  - clean-code
match:
  regex: return.*?\}.*?else\s*\{
  file_types:
    - js
    - ts
message: >-
  Unnecessary 'else' after 'return' at line {line}. Remove the else and flatten
  the code.
category: clean-code
