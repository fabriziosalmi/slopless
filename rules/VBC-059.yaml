id: VBC-059
name: file-length-limit
severity: warning
tags:
  - clean-code
match:
  ast_check:
    type: file-length-limit
    threshold: 500
message: >-
  File is too long ({count} lines). Consider splitting it into smaller modules.
  Max recommended: {threshold}.
category: core
