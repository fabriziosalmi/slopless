id: VBC-039
name: use-eval
severity: error
category: core
tags:
  - security
match:
  regex: eval\(
  file_types:
    - js
    - ts
message: >-
  Use of 'eval()' detected at line {line}. This is a massive security risk.
tests:
  fire:
    - eval(userInput);
  quiet:
    - JSON.parse(userInput);
