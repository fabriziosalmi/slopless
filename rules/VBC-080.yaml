id: VBC-080
name: browser-alert
severity: error
tags:
  - ux
match:
  regex: \balert\(
  file_types:
    - js
    - ts
message: Avoid using alert() at line {line}. Use a modern UI notification system.
category: clean-code
