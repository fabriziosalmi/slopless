id: VBC-026
name: empty-file
severity: warning
category: clean-code
tags:
  - clean-code
  - project-health
match:
  ast_check:
    type: empty-file
message: Empty code file detected. This clutters the repository.
tests:
  fire:
    - ''
    - "   \n"
  quiet:
    - export const answer = 42;
